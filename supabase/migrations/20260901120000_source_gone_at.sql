-- ═══════════════════════════════════════════════════════════════════════════
-- 원문 소멸 관측 시각 — 삭제 감지(SPEC §4 gone 단계)가 쓰는 칸 하나
--
-- ⚠️ **컬럼만 늘리면 크롤러가 깨진다.** 우리 직렬화는 `select *` + **컬럼 집합 엄격 대조**라
--    (`store/serde.py::_check_columns`) DB에 칸이 늘면 "잉여 컬럼"으로 보고 `review_data`
--    읽기가 전부 실패한다. 그래서 이 마이그레이션은 `models.ReviewData` 변경과 **한 묶음**이다.
--
-- ⚠️ `20260820234505_init.sql`을 고치지 않고 새 파일로 낸다 — 그 파일은 **이미 적용됐다**.
-- ═══════════════════════════════════════════════════════════════════════════

-- ── 원문이 게시판에서 사라진 것을 확인한 시각 — **크롤러가 쓰고 min_job이 읽는다** ──
--
-- 목록 대조로 추린 후보를 상세로 **한 번 더** 확인해 삭제가 확정된 때만 적는다(SPEC §4).
-- 공개된 공고면 크롤러가 같은 실행에서 `jobs.status='CLOSED'`로 내린다 — 이 칸은 그 근거다.
--
-- ⚠️ **판정이 아니라 관측 사실이다.** `review_status`는 건드리지 않는다 — 공개된 행은
--    `APPROVED`인 채 남고(그 불변식이 깨지면 publish가 혼란해진다), "왜 내려갔나"는 이 칸이
--    답한다. 같은 이유로 운영자 소유 가드(`is_operator_owned`)도 이 칸에는 적용하지 않는다:
--    사람의 교정을 덮는 것이 아니라 게시판에서 본 사실을 적는 것이다.
--
-- ⚠️ **비어 있음이 정상이다**(원문이 살아 있다). 값이 있는 행은 삭제 감지가 판정 대상에서
--    빼므로(매일 재확인하는 낭비 방지) 되살아나도 자동으로 다시 열지 않는다 — 그 결정은
--    운영자 몫이다(ROADMAP "되살아난 공고").
alter table review_data
  add column if not exists source_gone_at timestamptz;
